<h1>Editing cart_line_item</h1>

<%= error_messages_for :cart_line_item %>

<% form_for(:cart_line_item, :url => cart_line_item_path(@cart_line_item), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', cart_line_item_path(@cart_line_item) %> |
<%= link_to 'Back', cart_line_items_path %>